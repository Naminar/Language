
#ifndef LIST_
#define LIST_

#include <stdio.h>
#include <assert.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <limits.h>

#include "../include/node.h"

//===============================================

typedef const char* type;

typedef struct List{

    List* next;

    List* prev;
    
    
} List;

typedef struct Tree{

    List* lst      ;

    size_t capacity;

    size_t size    ;

} Tree;

//===============================================

void    list_destructor         (Tree*);
void    list_dump               (Tree*);


void    list_init               (Tree*, size_t);
bool    list_delete_after       (Tree*, size_t);
List*   search_element          (Tree*, size_t);

bool    list_insert             (Tree* tree, size_t num,Type a_type,
                                 Data a_data, const char* string_cell)

#endif // LIST_
